module.exports.getKnowledge = async (agentName) => {
  const base = ''
  if (agentName === 'Hilda') {
    return '';
  }

  if (agentName === 'Ajoke') {
    return '';
  }

  if (agentName === 'Ayeesha') {
    return '';
  }

  return '';
};
